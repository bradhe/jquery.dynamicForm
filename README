This is a jquery plugin that will turn any rails form in to an AJAX-enabled form! You can just include a tag on your form or call $.dynamicForm on the form you want to change.

So far the only types of messages that are supported are JSON messages, so your controllers must respond to JSON!